# Форма памяти сотрудника (шаблон — не заполнять здесь)

> Это шаблон формы для `team/agents/<имя>/memory.md` — память обычного агента (у коуча память —
> папка `memory/`, её форма — `team/agents/atlas/_memory-template/`). Заводится, когда агент
> разворачивается (Директор — этап 6; новые сотрудники — при найме). Правила канона: append-only ·
> даты ISO (`ГГГГ-ММ-ДД`) · свежее внизу · лимит 200 строк — разрослось → консолидация,
> устаревшее в `memory-archive.md` рядом (ничего не выбрасывается).

---

# Память <Имя>

## Лог
<!-- дата — что сделал/решил, на чём остановился (1-2 строки на заход) -->

## Решения
<!-- дата — решение и почему (чтобы не перерешивать заново) -->

## Паттерны
<!-- дата — что замечено про человека и его дело: факты, не диагнозы -->
