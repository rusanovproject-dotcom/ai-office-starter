# raw/ — сырьё как есть
Сюда ингест КЛАДЁТ принесённое как есть (исходники). ЖЕЛЕЗНО: агент здесь ничего не редактирует,
не генерит производное и не удаляет — только складывает оригиналы. Выжимку (../summary.md)
агент строит ПОВЕРХ, отдельным файлом, а не правкой сырья. Медиа — в raw/media/ (вне git).
